# GitHub OAuth App client ID (create one at https://github.com/settings/developers)
# Choose "OAuth App" (not GitHub App). The callback URL is not used (device flow),
# but GitHub requires one — use anything, e.g. https://example.com/callback.
# The app uses scopes: repo, workflow.
EXPO_PUBLIC_GITHUB_CLIENT_ID=your_client_id
